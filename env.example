# Runtime & App
NODE_ENV=development
APP_URL=http://localhost:3000
PORT=3000

# LLM / Providers
# Required for LLM via OpenRouter (leave empty to disable outbound LLM calls)
OPENROUTER_API_KEY=
# Optional; if set, Cohere is used for embeddings; otherwise Xenova (on-device)
COHERE_API_KEY=
# Default aligned with Dockerfile pre-download step
HUGGINGFACE_MODEL=Xenova/all-MiniLM-L6-v2

# Datastores
MONGODB_URI=mongodb://localhost:27017/rag
REDIS_URL=redis://localhost:6379

# Retrieval (ChromaDB)
# Set to "true" to enable Chroma retriever; otherwise in-memory retriever is used
USE_CHROMADB=false
CHROMA_URL=http://localhost:8000
# HNSW index params (higher = better recall, more resources)
CHROMA_HNSW_EF=200
CHROMA_HNSW_M=16

# Optional Node/SSL (useful in some containerized or corporate envs)
# NODE_EXTRA_CA_CERTS=/etc/ssl/certs/ca-certificates.crt
# NODE_TLS_REJECT_UNAUTHORIZED=0

# Optional Transformers cache (Dockerfile sets these in the image)
# TRANSFORMERS_CACHE=/app/.cache/huggingface
# HF_HOME=/app/.cache/huggingface
